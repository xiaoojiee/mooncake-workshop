# 月饼工坊（mooncake-workshop）

2D 前厅手艺 + 后厂放置的 H5 月饼小游戏，面向 **B站 Toy**。纯原生 Canvas 2D，无框架、无构建，逻辑分辨率 1280×720。

## 玩法（多客人并发 · 手忙脚乱）

**前厅（操作循环）**：同一时间店内会有多位客人，每人一张订单 + **独立耐心条**（倒计时）。玩家要在耐心耗尽前把月饼做出来。

制作是**拖拽组装**：
1. **拖面皮** → 放到制作台的某个空托盘
2. **拖馅料** → 依次叠到这张面上（一皮多馅，最多 4 层）
3. **拖整块月饼** → 到烤炉烤位烘烤
4. 烤好后**拖给对应客人** → 结算金币 / 口碑

拖到错误位置会自动退回，**不消耗库存**。

**后厂（经营循环）**：饼皮 / 馅料各为独立工厂，**实时生产**。营业中点「打开工厂取货」弹出**半透明面板（游戏不暂停，客人耐心继续走）**，手动把成品搬到原料架。

- 工厂库存 ≠ 原料架库存：**必须自己取货**，逼你在忙碌中抽空往返
- **工厂 = 槽位 + 组件**：每座工厂有若干槽位（初始 1 个，花钱开到 4 个）
- 槽位里装**组件**，组件决定生产速度 / 品质 / 产出，且**可单独升级**
- 烤位可升级扩容（初始 1 个，最多 4 个）——烤位是真正的瓶颈

## 食材与工厂（梗系）

| 类型 | 内容 |
|---|---|
| **饼皮 4 种** | 糖浆皮（开局）/ 奶黄皮 / 抹茶皮 / 巧克力皮，靠**开槽**解锁 |
| **馅料 8 种** | 五仁 + 莲蓉（开局）/ 豆沙 + 蛋黄（靠开槽）/ 西瓜 + 螺丝 + 砖头 + 腐肉（靠**梗组件**） |
| **工厂 12 座** | 4 座饼皮工坊 + 五仁/莲蓉/豆沙/蛋黄产线 + **瓜摊 / 五金车间 / 砖窑 / 刷怪塔** |

梗食材的解锁组件：

| 馅料 | 解锁组件 | 梗 |
|---|---|---|
| 西瓜 | 西瓜刀 | 华强买瓜「你这瓜保熟吗」 |
| 螺丝 | 螺母馅压机 | 公司发的月饼硬得能拧螺丝 |
| 砖头 | 高温窑炉 | 硌牙警告 |
| 腐肉 | 僵尸刷怪笼 | MC 造物，吃了掉饥饿值 |

## 素材（图集自动切割）

`assets/sprite/` 下 4 张图集，切图逻辑在 `src/core/atlas.js`：

| 文件 | 规格 | 对应 |
|---|---|---|
| `crust_sheet.png` | 3 行 × 4 列 | 行=生面团/包好成品/摊开饼皮；列=4 种饼皮 |
| `filling_a.png` | 2×2 | 五仁/莲蓉/豆沙/蛋黄 |
| `filling_b.png` | 2×2 | 西瓜/螺丝/砖头/腐肉 |
| `npc.png` | 上排3 + 下排2 | 5 位客人立绘 |

- **组装中**显示「摊开饼皮 + 馅料图标」；**包好/烤好**显示成品月饼，上方加**馅料气泡**（图标 + 数量）
- 详细清单见 `ASSETS.md`

## 组件系统

| 组件 | 稀有度 | 效果 | 获取 |
|---|---|---|---|
| 动力马达 | 普通 | +速度 | 购买 / 掉落 |
| 传动齿轮 | 普通 | +速度 +品质 | 购买 / 掉落 |
| 冷凝管 | 普通 | +品质 | 购买 / 掉落 |
| 精工模具 | 稀有 | +品质 | 购买 / 掉落 |
| 搅拌桨 | 稀有 | +速度 +产出 | 购买 / 掉落 |
| **螺母馅压机** | 特殊 | 解锁「螺母馅」 | **仅掉落** |
| 月光石 | 特殊 | 大量 +速度 +品质 | **仅掉落** |

- 普通/稀有组件可花金币购买，也可由特殊客人掉落
- 特殊组件**只能靠特殊客人掉落**
- 装了 `螺母馅压机` 才能生产梗食材「螺母馅」（公司发的月饼硬得能拧螺丝）
- 槽位越多，工厂库存上限越高（每多 1 槽 +30%）

## 食材解锁（混合体系）

| 类型 | 解锁方式 |
|---|---|
| 开局 | 广式糖浆皮 + **五仁** + **莲蓉** |
| 普通食材 | 对应工厂**开到指定槽数**自动解锁（酥皮 2 槽 / 豆沙 2 槽 / 奶黄 3 槽 / 桃山 3 槽 / 火腿 4 槽） |
| 限定食材 | B站 互动解锁：点赞 → 月兔印饼皮；投币 → 玉盘莲心馅；收藏 → 咸蛋黄流心；关注 → 冰皮 |
| 梗/道具食材 | 装上特殊组件解锁（螺母馅 ← 螺母馅压机） |

订单只会要求玩家**已解锁**的食材。

## 评分

```
面皮正确 0.15 + 馅料正确 0.35（种类 + 顺序）+ 火候 0.2 + 出餐速度 0.3（剩余耐心）
```
火候：偏生 < 正好 < 偏焦 < **烤糊（0 分）**。客人耐心耗尽 → 流失，扣金币与口碑。

**帮手与反派**（都靠「跑过去」干活，不是瞬移）：

- **月兔（花钱雇，最多 4 只）**：7 项能力各自升级（场上体型 = `RABBIT.bodyH` = 99）。纠错的月兔会把坏馅料**抱在头顶送回快捷栏**（一次一件），不是原地回收。闲下来且有人耐心告急时，会**主动在客人之间来回巡逻安抚**（最多 `RABBIT.calmMax` 只）
- **流浪猫「耄耋」（第 2 天起，免费）**：**每天只在随机时刻（12~95s）溜进来一次**，随机**扑咬客人**（客人当场没了，算流失）、随机**偷吃烤炉里没烤好的月饼**、随机**偷吃快捷栏材料**；吃东西时冒碎屑粒子（像 Minecraft）。烤好的月饼它不吃。**每干完一件坏事 80% 概率溜走**（溜了当天就不再来）
  - 玩家可以**直接拖动它**挪位置，也可以用**五金月饼投掷**打飞它
  - **打飞 3 次 → 第二天起变「驯服」状态**：不再捣乱，**一早就来上班**，在客人之间往返巡逻（耐心下降速度 ×0.2），还会**赶走找茬的刘华强**（不掉玩家口碑）
  - **刘华强耐心条无限**（画金色满格 + ∞，只能靠驯服的耄耋赶走）

**特殊客人**（`defs.js` 的 `kind:'special'`，`order.kind` 定订单规则、`onServe` 定出餐奖励，可挂多条）：

| 客人 | 怪癖 | 出餐奖励 |
|---|---|---|
| 嫦娥 | 普通点单 | 首次送月兔模具 |
| 刘华强 | 30 份西瓜馅（做不出来）+ `harass` | 五金月饼打飞掉「西瓜刀」 |
| 良子 | 馅料 3 倍（有腐肉就全腐肉） | `coinMul: 2` **工钱翻倍** |
| 月兔 | 普通点单 | `restorePatience` 恢复所有客人耐心 + `rabbitGift` **有几率送一只月兔 / 给现有月兔随机升 1 级**（几率 = `DAY.rabbitGiftChance`） |
| 史蒂夫 | 只点腐肉（没上场就只要饼皮） | `freeUpgrade` **随机白送一项升级**（工厂槽位/制作台/烤炉/锅炉/能源核心，**月兔除外**） |

## 目录结构

- `index.html` 入口，按依赖顺序加载 `src/` 下全部脚本（经典 `<script>`，全局共享作用域）
- `src/core/` utils / config(常量) / defs(食材·工厂·组件·订单表) / state(全局状态) / canvas / assets / input / audio / toy(SDK)
- `src/systems/` factory(槽位·组件·产线·取货) / assembly(拖拽组装·评分) / order(客人并发·耐心·结算·掉落) / rabbit(月兔帮手) / cat(流浪猫耄耋) / save(存档)
- `src/scenes/` boot / loading / menu / **shop(营业主场景)** / factory(工厂管理) / rank
- `src/ui/` hud(按钮·面板·背景) / rabbit(月兔面板) / factoryboard(工厂网格)
- `src/fx/` steam / floattext / coinfly(拾取金币飞入顶栏) / crumb(吃东西的碎屑粒子)
- `assets/` 游戏贴图与音频，见 `ASSETS.md`
- `docs/` 参考文档

## 命令

- 本地运行：`npx serve .`（**不能双击 index.html**，贴图需 http 协议）
- 模拟 B站 互动：`http://localhost:3000/?mock=1`，控制台执行 `__toyMock.toggle('liked')` 等
- 无 lint/构建步骤（有意为之，与 demo1 保持一致）

## B站 Toy 能力取舍

| 能力 | 用途 |
|---|---|
| `getCloudStorage` / `setCloudStorage` | 存档。按「登录用户 + Toy」隔离、跨设备同步，**不弹数据确认**。单 value ≤1024 字节，故拆成 `meta` / `factory` / `progress` 三 key |
| `submitScore` / `getRankList` / `getMyRank` | 三个榜位：`1` 金币榜 / `2` 评分榜 / `3` 口碑榜。是否上榜必须用 `ranked` 字段 |
| `getVideoUserActions` / `getAuthorRelation` | 互动解锁食材（端内外均支持，不弹数据确认） |
| `setContainerMode` / `onContainerChange` | 请求横屏 + 沉浸。**手机不支持"横屏且非沉浸"组合**，故必须 `immersive: true`；`Promise` 不是成功回执 |
| 未使用 | `getUserProfile`（会弹数据确认）、`saveImageToAlbum` / `share` / `closeBrowser`（仅 App 内）、摄像头/麦克风（与玩法无关） |

非 Toy 环境自动降级：云存储回退 localStorage，互动解锁全部为 false 但可用 `?mock=1` 模拟。

## 约定

- 跨文件引用全局变量/函数时，在文件顶部用 `/* global xxx */` 注释声明
- 贴图尺寸按 `naturalWidth/Height` 动态计算，不写死
- 缺失贴图不阻塞游戏，自动绘制虚线占位框（带 key 文字），便于对齐美术
- 新增素材：在 `src/core/config.js` 的 `ASSET_LIST` 加一项即可
- 新增饼皮 / 馅料 / 工厂：改 `src/core/defs.js`
- 数值调优集中在 `src/core/config.js` 与 `defs.js`
- 只有用户明确说「提交 git / 上传」时才 commit / push
- **助手的走路目标必须用「稳定锚点」**（`ovenAnchorPoint()` / `shelfAnchorPoint()`），不能用 `ovenRect(i)` / `hotbarItems()` 的 rect：那些带面板滚动偏移，玩家一滚助手就跑偏、滚出可视区还会够不着而放弃任务
- 助手（月兔/耄耋）都有**防卡死兜底**：拖拽状态丢了自动松手、够不着的目标 2.5~3s 后放弃、坐标变 NaN 就地救回。新加会「走过去」的行为时沿用这套（`rabbitWalkTo` / `catWalk` 里的看门狗）
- **别用 PowerShell 的 `Get-Content` / `Set-Content` 改这些 UTF-8 源码**：PS 5.1 会按 GBK 往返，中文会被写成 `?` 并吞掉后面的字节（引号/换行）。要改文件就用 `edit`/`write` 工具，或写 Node 脚本用 `fs.readFileSync/writeFileSync(...,'utf8')`

## 当前状态

**框架 + 可玩垂直切片已完成**：多客人并发营业、拖拽组装、烤炉、半透明工厂取货、工厂槽位与组件、渐进解锁、三榜位、云存档、互动解锁、特殊客人掉落组件、月兔帮手（分工）、流浪猫耄耋、柜台摆盘、五金月饼、拾取金币飞入。
**待接美术**：`assets/` 下贴图全部为占位状态，清单与规格见 `ASSETS.md`（流浪猫是程序化画的，没占贴图）。
**已知待办**：云存档 `progress` 在后期超过单 key 1024 字节上限（需压缩字段 / 拆 key）。
